# GuardClaw — Python dependencies
# Install: pip install -r requirements.txt

# ── Core (required) ────────────────────────────────────────────────
customtkinter>=5.2.0   # Modern dark-theme UI framework
pyperclip>=1.8.2       # Cross-platform clipboard access
requests>=2.28.0       # HTTP client for Ollama API

# ── Optional: async support ────────────────────────────────────────
# Enables query_ollama_async() for asyncio-based agent integrations
# (OpenClaw gateway, FastAPI middleware, etc.)
# httpx>=0.27.0

# ── Optional: encrypted history ────────────────────────────────────
# Required for AES-256 history encryption (roadmap feature)
# cryptography>=42.0.0

# ── Development / testing ─────────────────────────────────────────
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
